      {/* Quick Status */}
      {homeModules.quickStatus && (
      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr 1fr", gap: 8, marginBottom: 20 }}>
        {[
          { icon: <BedDouble size={20} color="#6366f1" />, value: `${sleepHours}h`, label: "שינה" },
          { icon: <Footprints size={20} color="#22c55e" />, value: todaySteps || "—", label: "צעדים" },
          { icon: <Droplets size={20} color="#3b82f6" />, value: waterCups, label: "כוסות מים" },
          { icon: <Pill size={20} color="#f59e0b" />, value: todaySupplements.length, label: "ויטמינים" },
        ].map((s, i) => (
          <div key={i} style={{ background: "#fff", borderRadius: 14, padding: 12, textAlign: "center", boxShadow: "0 2px 8px rgba(0,0,0,0.06)" }}>
            <div style={{ marginBottom: 2 }}>{s.icon}</div>
            <div style={{ fontSize: 16, fontWeight: 700, color: "#1e293b" }}>{s.value}</div>
            <div style={{ fontSize: 10, color: "#94a3b8" }}>{s.label}</div>
          </div>
        ))}
      </div>
      )}

      {/* Water Tracker (if tracking) */}
      {homeModules.water && (
        <div style={{ background: "#fff", borderRadius: 16, padding: 18, marginBottom: 16, boxShadow: "0 2px 8px rgba(0,0,0,0.06)" }}>
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 10 }}>
            <div style={{ display: "flex", alignItems: "center", gap: 6 }}>
              <button onClick={() => setWaterCups(Math.max(0, waterCups - 1))}
                style={{ width: 32, height: 32, borderRadius: 8, border: "2px solid #e2e8f0", background: "#fff", cursor: "pointer", fontSize: 18, color: "#64748b" }}>−</button>
              <span style={{ fontSize: 20, fontWeight: 700, color: "#3b82f6", minWidth: 30, textAlign: "center" }}>{waterCups}</span>
              <button onClick={() => setWaterCups(waterCups + 1)}
                style={{ width: 32, height: 32, borderRadius: 8, border: "none", background: "#3b82f6", color: "#fff", cursor: "pointer", fontSize: 18, fontWeight: 700 }}>+</button>
            </div>
            <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
              <span style={{ fontSize: 15, fontWeight: 700, color: "#1e293b" }}>מים 💧</span>
            </div>
          </div>
          <ProgressBar value={waterCups} max={8} color="#3b82f6" label="יעד: 8 כוסות" unit="" />
        </div>
      )}

      {/* Questionnaire Status — compact cards linking to the שאלונים tab */}
      {homeModules.questionnaires && (
      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10, marginBottom: 16 }}>
        <button onClick={() => { if (!morningDone) setActiveTab("questionnaires"); }}
          style={{ background: morningDone ? "#f0fdf4" : "#fff", borderRadius: 14, padding: 14, border: morningDone ? "2px solid #86efac" : "2px solid #fbbf24", cursor: morningDone ? "default" : "pointer", textAlign: "right", boxShadow: "0 2px 8px rgba(0,0,0,0.06)" }}>
          <div style={{ display: "flex", alignItems: "center", gap: 6, justifyContent: "flex-end", marginBottom: 6 }}>
            <span style={{ fontWeight: 700, fontSize: 14, color: morningDone ? "#166534" : "#92400e" }}>שאלון בוקר</span>
            <span>{morningDone ? "✅" : "☀️"}</span>
          </div>
          <div style={{ fontSize: 12, color: morningDone ? "#166534" : "#b45309" }}>
            {morningDone ? `הושלם — ${sleepHours} שעות שינה` : "ממתין למילוי →"}
          </div>
        </button>
        <button onClick={() => { if (!eveningDone) setActiveTab("questionnaires"); }}
          style={{ background: eveningDone ? "#f5f3ff" : "#fff", borderRadius: 14, padding: 14, border: eveningDone ? "2px solid #c4b5fd" : "2px solid #e2e8f0", cursor: eveningDone ? "default" : "pointer", textAlign: "right", boxShadow: "0 2px 8px rgba(0,0,0,0.06)" }}>
          <div style={{ display: "flex", alignItems: "center", gap: 6, justifyContent: "flex-end", marginBottom: 6 }}>
            <span style={{ fontWeight: 700, fontSize: 14, color: eveningDone ? "#4c1d95" : "#475569" }}>שאלון ערב</span>
            <span>{eveningDone ? "✅" : "🌙"}</span>
          </div>
          <div style={{ fontSize: 12, color: eveningDone ? "#4c1d95" : "#64748b" }}>
            {eveningDone ? "הושלם" : "ימולא בערב →"}
          </div>
        </button>
      </div>
      )}

      {/* Quick health report button */}
      {homeModules.healthReport && (
      <button onClick={() => setActiveTab("questionnaires")}
        style={{ width: "100%", padding: 14, borderRadius: 14, border: "2px solid #fecaca", background: "#fff", color: "#dc2626", fontSize: 14, fontWeight: 700, cursor: "pointer", display: "flex", alignItems: "center", justifyContent: "center", gap: 8, marginBottom: 16, boxShadow: "0 2px 8px rgba(0,0,0,0.04)" }}>
        <Stethoscope size={18} /> דיווח בריאות חופשי
        {healthReports.length > 0 && <span style={{ background: "#dc2626", color: "#fff", borderRadius: 10, padding: "2px 8px", fontSize: 11, fontWeight: 700 }}>{healthReports.length}</span>}
      </button>
      )}

      {/* Food Log — MFP Style */}
      {homeModules.foodLog && (
      <div style={{ background: "#fff", borderRadius: 16, padding: 20, marginBottom: 16, boxShadow: "0 2px 8px rgba(0,0,0,0.06)" }}>
        {/* Calorie ring + macros summary */}
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 16 }}>
          <div style={{ display: "flex", gap: 16 }}>
            {[
              { label: "חלבון", value: todayTotals.protein, goal: proteinGoal, color: "#ef4444", unit: "g" },
              { label: "פחמימות", value: todayTotals.carbs, goal: Math.round(calorieGoal * 0.45 / 4), color: "#3b82f6", unit: "g" },
              { label: "שומן", value: todayTotals.fat, goal: Math.round(calorieGoal * 0.3 / 9), color: "#f59e0b", unit: "g" },
            ].map((m, i) => (
              <div key={i} style={{ textAlign: "center" }}>
                <div style={{ fontSize: 16, fontWeight: 700, color: m.color }}>{m.value}</div>
                <div style={{ fontSize: 10, color: "#94a3b8" }}>/{m.goal}{m.unit}</div>
                <div style={{ fontSize: 10, color: "#64748b" }}>{m.label}</div>
              </div>
            ))}
          </div>
          {/* Calorie circle */}
          <div style={{ position: "relative", width: 80, height: 80 }}>
            <svg width="80" height="80" viewBox="0 0 80 80" style={{ transform: "rotate(-90deg)" }}>
              <circle cx="40" cy="40" r="34" fill="none" stroke="#f1f5f9" strokeWidth="6" />
              <circle cx="40" cy="40" r="34" fill="none" stroke="#6366f1" strokeWidth="6"
                strokeDasharray={`${Math.min((todayTotals.calories / calorieGoal) * 213.6, 213.6)} 213.6`}
                strokeLinecap="round" />
            </svg>
            <div style={{ position: "absolute", inset: 0, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center" }}>
              <div style={{ fontSize: 16, fontWeight: 800, color: "#1e293b" }}>{Math.max(0, calorieGoal - todayTotals.calories)}</div>
              <div style={{ fontSize: 8, color: "#94a3b8" }}>נותרו קק״ל</div>
            </div>
          </div>
        </div>

        {/* Add food button */}
        <button onClick={() => setShowFoodSearch(true)}
          style={{ width: "100%", padding: 14, borderRadius: 12, border: "2px dashed #c7d2fe", background: "#eef2ff", color: "#6366f1", fontSize: 15, fontWeight: 700, cursor: "pointer", display: "flex", alignItems: "center", justifyContent: "center", gap: 8, marginBottom: 16, transition: "all 0.2s" }}>
          <Plus size={20} /> הוסף מאכל
        </button>

        {/* Today's food list */}
        {todayFoods.length > 0 && (
          <div>
            <div style={{ fontSize: 13, fontWeight: 600, color: "#64748b", marginBottom: 8, textAlign: "right" }}>
              מה אכלתי היום ({todayFoods.length} מאכלים)
            </div>
            {todayFoods.map(entry => (
              <div key={entry.id} style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "12px 0", borderBottom: "1px solid #f1f5f9" }}>
                <div style={{ display: "flex", alignItems: "center", gap: 6 }}>
                  <button onClick={() => removeFood(entry.id)} style={{ background: "none", border: "none", cursor: "pointer", color: "#cbd5e1", padding: 2 }}>
                    <X size={16} />
                  </button>
                  <div style={{ textAlign: "left", minWidth: 50 }}>
                    <div style={{ fontSize: 14, fontWeight: 700, color: "#6366f1" }}>{Math.round(entry.food.calories * entry.servings)}</div>
                    <div style={{ fontSize: 9, color: "#94a3b8" }}>קק״ל</div>
                  </div>
                </div>
                <div style={{ flex: 1, textAlign: "right", paddingRight: 8 }}>
                  <div style={{ fontSize: 14, fontWeight: 600, color: "#1e293b", display: "flex", justifyContent: "flex-end", gap: 6, alignItems: "center" }}>
                    <span style={{ fontSize: 10, color: "#94a3b8", fontWeight: 500, background: "#f1f5f9", padding: "2px 6px", borderRadius: 10 }}>🕐 {entry.time}</span>
                    <span>{entry.food.name}</span>
                  </div>
                  <div style={{ fontSize: 11, color: "#94a3b8", display: "flex", gap: 4, justifyContent: "flex-end", alignItems: "center" }}>
                    <span>{entry.food.serving || ""}</span>
                    {entry.servings !== 1 && <span style={{ color: "#6366f1", fontWeight: 600 }}>×{entry.servings}</span>}
                    <span style={{ marginRight: 6 }}>•</span>
                    <span>ח׳{Math.round(entry.food.protein * entry.servings)}  פ׳{Math.round(entry.food.carbs * entry.servings)}  ש׳{Math.round(entry.food.fat * entry.servings)}</span>
                  </div>
                </div>
                {/* Serving adjuster */}
                <div style={{ display: "flex", alignItems: "center", gap: 4, marginLeft: 4 }}>
                  <button onClick={() => updateServings(entry.id, Math.max(0.5, entry.servings - 0.5))}
                    style={{ width: 24, height: 24, borderRadius: 6, border: "1px solid #e2e8f0", background: "#fff", cursor: "pointer", fontSize: 14, color: "#64748b", display: "flex", alignItems: "center", justifyContent: "center" }}>−</button>
                  <span style={{ fontSize: 12, fontWeight: 600, color: "#475569", minWidth: 20, textAlign: "center" }}>{entry.servings}</span>
                  <button onClick={() => updateServings(entry.id, entry.servings + 0.5)}
                    style={{ width: 24, height: 24, borderRadius: 6, border: "none", background: "#6366f1", color: "#fff", cursor: "pointer", fontSize: 14, display: "flex", alignItems: "center", justifyContent: "center" }}>+</button>
                </div>
              </div>
            ))}
            {/* Daily total bar */}
            <div style={{ display: "flex", justifyContent: "space-between", marginTop: 12, padding: "10px 0", borderTop: "2px solid #e2e8f0" }}>
              <div style={{ fontSize: 14, fontWeight: 700, color: "#6366f1" }}>{todayTotals.calories} קק״ל</div>
              <div style={{ fontSize: 14, fontWeight: 700, color: "#1e293b" }}>סה״כ היום</div>
            </div>
          </div>
        )}

        {todayFoods.length === 0 && (
          <div style={{ textAlign: "center", padding: 20, color: "#94a3b8" }}>
            <UtensilsCrossed size={28} style={{ marginBottom: 6, opacity: 0.4 }} />
            <div style={{ fontSize: 13 }}>עוד לא הזנת מאכלים היום</div>
            <div style={{ fontSize: 11 }}>לחצ/י למעלה כדי להוסיף</div>
          </div>
        )}
      </div>
      )}

      {/* Supplements */}
      {homeModules.supplements && (
      <div style={{ background: "#fff", borderRadius: 16, padding: 20, marginBottom: 16, boxShadow: "0 2px 8px rgba(0,0,0,0.06)" }}>
        <div style={{ fontSize: 16, fontWeight: 700, color: "#1e293b", marginBottom: 12, textAlign: "right", display: "flex", alignItems: "center", gap: 8, justifyContent: "flex-end" }}>
          <span>ויטמינים ותוספים</span>
          <Pill size={20} color="#f59e0b" />
        </div>
        <div style={{ display: "flex", flexWrap: "wrap", gap: 8, justifyContent: "flex-end" }}>
          {(userProfile?.currentSupplements?.length > 0 ? userProfile.currentSupplements : SUPPLEMENTS).map(sup => {
            const taken = todaySupplements.includes(sup);
            const logEntry = supplementLog.find(l => l.name === sup);
            return (
              <button key={sup} onClick={() => toggleSupplement(sup)}
                style={{
                  padding: "8px 14px", borderRadius: 20, border: "2px solid",
                  borderColor: taken ? "#22c55e" : "#e2e8f0",
                  background: taken ? "#f0fdf4" : "#fff",
                  color: taken ? "#166534" : "#64748b",
                  cursor: "pointer", fontSize: 13, fontWeight: 500,
                  display: "flex", alignItems: "center", gap: 4
                }}>
                <span>{sup}</span>
                {taken && <Check size={14} />}
                {taken && logEntry && <span style={{ fontSize: 10, color: "#15803d", opacity: 0.8 }}>🕐{logEntry.time}</span>}
              </button>
            );
          })}
          {/* "אחר" button to open full supplement picker */}
          <button onClick={() => setShowSupplementPicker(true)}
            style={{
              padding: "8px 14px", borderRadius: 20, border: "2px dashed #f59e0b",
              background: "#fffbeb", color: "#d97706",
              cursor: "pointer", fontSize: 13, fontWeight: 600,
              display: "flex", alignItems: "center", gap: 4
            }}>
            <Plus size={14} />
            <span>תוסף אחר</span>
          </button>
        </div>
        {/* Extra supplements taken that aren't in the default list */}
        {todaySupplements.filter(s => !(userProfile?.currentSupplements?.length > 0 ? userProfile.currentSupplements : SUPPLEMENTS).includes(s)).length > 0 && (
          <div style={{ marginTop: 10, paddingTop: 10, borderTop: "1px solid #f1f5f9" }}>
            <div style={{ fontSize: 11, fontWeight: 600, color: "#94a3b8", marginBottom: 6, textAlign: "right" }}>תוספים נוספים שנלקחו היום:</div>
            <div style={{ display: "flex", flexWrap: "wrap", gap: 6, justifyContent: "flex-end" }}>
              {todaySupplements.filter(s => !(userProfile?.currentSupplements?.length > 0 ? userProfile.currentSupplements : SUPPLEMENTS).includes(s)).map(sup => (
                <button key={sup} onClick={() => toggleSupplement(sup)}
                  style={{ padding: "6px 12px", borderRadius: 20, border: "2px solid #22c55e", background: "#f0fdf4", color: "#166534", cursor: "pointer", fontSize: 12, fontWeight: 500, display: "flex", alignItems: "center", gap: 4 }}>
                  <span>{sup}</span>
                  <Check size={12} />
                </button>
              ))}
            </div>
          </div>
        )}
      </div>
      )}

      {/* Supplement Picker Modal */}
      {showSupplementPicker && (
        <div style={{ position: "fixed", inset: 0, background: "#fff", zIndex: 1000, display: "flex", flexDirection: "column", maxWidth: 480, margin: "0 auto" }}>
          <div style={{ background: "linear-gradient(135deg, #f59e0b, #d97706)", padding: "16px", color: "#fff" }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 12 }}>
              <button onClick={() => { setShowSupplementPicker(false); setSupplementSearch(""); }}
                style={{ background: "none", border: "none", cursor: "pointer", color: "#fff", fontSize: 14, fontWeight: 600 }}>
                ✕ סגור
              </button>
              <span style={{ fontSize: 18, fontWeight: 700 }}>💊 כל התוספים והויטמינים</span>
            </div>
            <div style={{ position: "relative" }}>
              <input type="text" placeholder="חפש תוסף..." value={supplementSearch} onChange={e => setSupplementSearch(e.target.value)} autoFocus
                style={{ width: "100%", padding: "10px 40px 10px 10px", borderRadius: 12, border: "none", fontSize: 14, textAlign: "right", direction: "rtl", boxSizing: "border-box" }} />
              <Search size={16} color="#94a3b8" style={{ position: "absolute", right: 12, top: "50%", transform: "translateY(-50%)" }} />
            </div>
          </div>

          <div style={{ flex: 1, overflow: "auto", padding: "12px 16px", direction: "rtl" }}>
            {/* Custom add */}
            <div style={{ padding: "12px 0", borderBottom: "1px solid #e2e8f0", marginBottom: 12 }}>
              <div style={{ fontSize: 13, fontWeight: 600, color: "#64748b", marginBottom: 6 }}>הוסף תוסף מותאם אישית:</div>
              <div style={{ display: "flex", gap: 8 }}>
                <input type="text" placeholder="שם התוסף..." value={customSupplementInput} onChange={e => setCustomSupplementInput(e.target.value)}
                  onKeyDown={e => { if (e.key === "Enter" && customSupplementInput.trim()) { toggleSupplement(customSupplementInput.trim()); setCustomSupplementInput(""); }}}
                  style={{ flex: 1, padding: 10, borderRadius: 10, border: "2px solid #e2e8f0", fontSize: 13, textAlign: "right" }} />
                <button onClick={() => { if (customSupplementInput.trim()) { toggleSupplement(customSupplementInput.trim()); setCustomSupplementInput(""); }}}
                  style={{ padding: "10px 14px", borderRadius: 10, border: "none", background: "#f59e0b", color: "#fff", cursor: "pointer", fontWeight: 600 }}>
                  <Plus size={16} />
                </button>
              </div>
            </div>

            {/* Grouped supplements */}
            {ALL_SUPPLEMENTS.map(group => {
              const filteredItems = supplementSearch
                ? group.items.filter(item => item.includes(supplementSearch))
                : group.items;
              if (filteredItems.length === 0) return null;
              return (
                <div key={group.group} style={{ marginBottom: 16 }}>
                  <div style={{ fontSize: 13, fontWeight: 700, color: "#92400e", marginBottom: 8, display: "flex", alignItems: "center", gap: 4 }}>
                    {group.group}
                  </div>
                  <div style={{ display: "flex", flexWrap: "wrap", gap: 6 }}>
                    {filteredItems.map(sup => {
                      const taken = todaySupplements.includes(sup);
                      return (
                        <button key={sup} onClick={() => toggleSupplement(sup)}
                          style={{
                            padding: "7px 12px", borderRadius: 20, border: "2px solid",
                            borderColor: taken ? "#22c55e" : "#e2e8f0",
                            background: taken ? "#f0fdf4" : "#fff",
                            color: taken ? "#166534" : "#64748b",
                            cursor: "pointer", fontSize: 12, fontWeight: 500,
                            display: "flex", alignItems: "center", gap: 4
                          }}>
                          <span>{sup}</span>
                          {taken && <Check size={12} />}
                        </button>
                      );
                    })}
                  </div>
                </div>
              );
            })}
            <div style={{ height: 20 }} />
          </div>

          {/* Bottom summary */}
          {todaySupplements.length > 0 && (
            <div style={{ padding: "12px 16px", borderTop: "1px solid #e2e8f0", background: "#f0fdf4", direction: "rtl" }}>
              <div style={{ fontSize: 13, fontWeight: 700, color: "#166534", marginBottom: 4 }}>
                ✅ נלקחו היום: {todaySupplements.length} תוספים
              </div>
              <div style={{ fontSize: 11, color: "#15803d" }}>{todaySupplements.join(" • ")}</div>
            </div>
          )}
        </div>
      )}

      {/* Symptom Picker Modal */}
      {showSymptomPicker && (
        <div style={{ position: "fixed", inset: 0, background: "#fff", zIndex: 1000, display: "flex", flexDirection: "column", maxWidth: 480, margin: "0 auto" }}>
          <div style={{ background: "linear-gradient(135deg, #ef4444, #dc2626)", padding: "16px", color: "#fff" }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 12 }}>
              <button onClick={() => { setShowSymptomPicker(false); setHomeSymptomSearch(""); }}
                style={{ background: "none", border: "none", cursor: "pointer", color: "#fff", fontSize: 14, fontWeight: 600 }}>
                ✕ סגור
              </button>
              <span style={{ fontSize: 18, fontWeight: 700 }}>🩺 כל התסמינים והמיחושים</span>
            </div>
            <div style={{ position: "relative" }}>
              <input type="text" placeholder="חפש תסמין..." value={homeSymptomSearch} onChange={e => setHomeSymptomSearch(e.target.value)} autoFocus
                style={{ width: "100%", padding: "10px 40px 10px 10px", borderRadius: 12, border: "none", fontSize: 14, textAlign: "right", direction: "rtl", boxSizing: "border-box" }} />
              <Search size={16} color="#94a3b8" style={{ position: "absolute", right: 12, top: "50%", transform: "translateY(-50%)" }} />
            </div>
          </div>

          <div style={{ flex: 1, overflow: "auto", padding: "12px 16px", direction: "rtl" }}>
            {/* Free text add */}
            <div style={{ padding: "10px 0", borderBottom: "1px solid #e2e8f0", marginBottom: 12 }}>
              <div style={{ fontSize: 12, fontWeight: 600, color: "#64748b", marginBottom: 6 }}>הוסף תסמין מותאם אישית:</div>
              <div style={{ display: "flex", gap: 8 }}>
                <input type="text" placeholder="תאר/י את התסמין..." value={newSymptom} onChange={e => setNewSymptom(e.target.value)}
                  onKeyDown={e => { if (e.key === "Enter" && newSymptom.trim()) { setSymptoms(prev => [...prev, { id: Date.now(), text: newSymptom.trim(), icon: "📝", time: new Date().toLocaleTimeString("he-IL", { hour: "2-digit", minute: "2-digit" }), timestamp: Date.now() }]); setNewSymptom(""); }}}
                  style={{ flex: 1, padding: 10, borderRadius: 10, border: "2px solid #e2e8f0", fontSize: 13, textAlign: "right" }} />
                <button onClick={() => { if (newSymptom.trim()) { setSymptoms(prev => [...prev, { id: Date.now(), text: newSymptom.trim(), icon: "📝", time: new Date().toLocaleTimeString("he-IL", { hour: "2-digit", minute: "2-digit" }), timestamp: Date.now() }]); setNewSymptom(""); }}}
                  style={{ padding: "10px 14px", borderRadius: 10, border: "none", background: "#ef4444", color: "#fff", cursor: "pointer", fontWeight: 600 }}>
                  <Plus size={16} />
                </button>
              </div>
            </div>

            {/* Grouped symptoms */}
            {SYMPTOM_GROUPS.map(group => {
              const groupItems = SYMPTOM_TYPES.filter(s => s.group === group);
              const filtered = homeSymptomSearch ? groupItems.filter(s => s.label.includes(homeSymptomSearch)) : groupItems;
              if (filtered.length === 0) return null;
              return (
                <div key={group} style={{ marginBottom: 14 }}>
                  <div style={{ fontSize: 13, fontWeight: 700, color: "#991b1b", marginBottom: 6 }}>{group}</div>
                  <div style={{ display: "flex", flexWrap: "wrap", gap: 5 }}>
                    {filtered.map(s => {
                      const active = symptoms.some(sym => sym.typeId === s.id);
                      return (
                        <button key={s.id} onClick={() => {
                          if (!active) {
                            setSymptoms(prev => [...prev, { id: Date.now(), typeId: s.id, text: s.label, icon: s.icon, time: new Date().toLocaleTimeString("he-IL", { hour: "2-digit", minute: "2-digit" }), timestamp: Date.now() }]);
                          } else {
                            setSymptoms(prev => prev.filter(sym => sym.typeId !== s.id));
                          }
                        }}
                          style={{
                            padding: "6px 10px", borderRadius: 20, border: "2px solid",
                            borderColor: active ? "#ef4444" : "#e2e8f0",
                            background: active ? "#fef2f2" : "#fff",
                            color: active ? "#dc2626" : "#64748b",
                            cursor: "pointer", fontSize: 11, fontWeight: 500,
                            display: "flex", alignItems: "center", gap: 3
                          }}>
                          {active && <Check size={11} />}
                          <span>{s.icon}</span>
                          <span>{s.label}</span>
                        </button>
                      );
                    })}
                  </div>
                </div>
              );
            })}
            <div style={{ height: 20 }} />
          </div>

          {/* Bottom summary */}
          {symptoms.length > 0 && (
            <div style={{ padding: "12px 16px", borderTop: "1px solid #e2e8f0", background: "#fef2f2", direction: "rtl" }}>
              <div style={{ fontSize: 13, fontWeight: 700, color: "#991b1b", marginBottom: 4 }}>
                🩺 דווחו היום: {symptoms.length} תסמינים
              </div>
              <div style={{ fontSize: 11, color: "#dc2626" }}>{symptoms.map(s => `${s.icon || ""} ${s.text}`).join(" • ")}</div>
            </div>
          )}
        </div>
      )}

      {/* Exercise */}
      {homeModules.exercise && (
      <div style={{ background: "#fff", borderRadius: 16, padding: 20, marginBottom: 16, boxShadow: "0 2px 8px rgba(0,0,0,0.06)" }}>
        <div style={{ fontSize: 16, fontWeight: 700, color: "#1e293b", marginBottom: 12, textAlign: "right", display: "flex", alignItems: "center", gap: 8, justifyContent: "flex-end" }}>
          <span>פעילות גופנית</span>
          <Activity size={20} color="#22c55e" />
        </div>
        <PillToggle items={EXERCISE_TYPES} selected={todayExercise} onToggle={id => setTodayExercise(todayExercise === id ? null : id)} multi={false} />
        {homeModules.steps && (
          <div style={{ marginTop: 12 }}>
            <input type="number" placeholder="כמה צעדים היום?" value={todaySteps} onChange={e => setTodaySteps(e.target.value)}
              style={{ width: "100%", padding: 12, borderRadius: 10, border: "2px solid #e2e8f0", fontSize: 14, textAlign: "right", direction: "rtl", boxSizing: "border-box" }} />
          </div>
        )}
      </div>
      )}

      {/* Period Tracking */}
      {homeModules.period && (
        <div style={{ background: "#fff", borderRadius: 16, padding: 20, marginBottom: 16, boxShadow: "0 2px 8px rgba(0,0,0,0.06)" }}>
          <label style={{ display: "flex", alignItems: "center", gap: 10, cursor: "pointer", justifyContent: "flex-end", fontSize: 15, fontWeight: 600, color: "#1e293b" }}>
            <input type="checkbox" checked={periodToday} onChange={() => {
              const todayStr = new Date().toISOString().split("T")[0];
              if (!periodToday) {
                setPeriodLog(prev => prev.includes(todayStr) ? prev : [...prev, todayStr]);
              } else {
                setPeriodLog(prev => prev.filter(d => d !== todayStr));
              }
              setPeriodToday(!periodToday);
            }}
              style={{ width: 20, height: 20, accentColor: "#ec4899" }} />
            <span>מחזור חודשי היום</span>
            <Droplets size={20} color="#ec4899" />
          </label>
        </div>
      )}

      {/* Health Symptoms */}
      {homeModules.symptoms && (
      <div style={{ background: "#fff", borderRadius: 16, padding: 20, marginBottom: 16, boxShadow: "0 2px 8px rgba(0,0,0,0.06)" }}>
        <div style={{ fontSize: 16, fontWeight: 700, color: "#1e293b", marginBottom: 12, textAlign: "right", display: "flex", alignItems: "center", gap: 8, justifyContent: "flex-end" }}>
          <span>תופעות בריאותיות ומיחושים</span>
          <AlertTriangle size={20} color="#ef4444" />
        </div>